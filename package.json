{
  "name": "use-table-reform",
  "version": "1.0.0",
  "description": "Chicago zoning use table lookup — What's Banned on Your Block?",
  "type": "module",
  "scripts": {
    "dev": "serve -p 8000 .",
    "build": "echo \"export const MAPBOX_TOKEN = \\\"$MAPBOX_TOKEN\\\";\" > js/config.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@turf/bbox": "^6.5.0",
    "@turf/boolean-point-in-polygon": "^6.5.0",
    "@turf/distance": "^6.5.0",
    "@turf/helpers": "^6.5.0",
    "serve": "^14.2.6",
    "vitest": "^2.0.0"
  }
}
